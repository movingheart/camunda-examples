# camunda-examples 运行依赖
# ─────────────────────────────────────────────────────
# camunda-python 引擎：从 PyPI 安装正式版（0.1.2 起支持解析 userTask
# 节点变量：camunda:assignee / candidateUsers / candidateGroups 在任务
# 创建时对 ${...} 表达式求值）
#   pip install -r requirements.txt
camunda-python[api]>=0.1.2

# Web 框架
fastapi>=0.110
uvicorn[standard]>=0.27

# ORM（camunda-python 自身也用，但 Biz 业务层直接 import 了）
sqlalchemy>=2.0

# MySQL 驱动（不安装也能跑——内置默认 SQLite）
pymysql>=1.1

# BPMN XML 解析
lxml>=4.9
